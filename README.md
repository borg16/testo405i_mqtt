# testo405i_mqtt
A Python utility that connects to the **Testo 405i** anemometer via Bluetooth Low Energy (BLE),   reads **temperature** and **air velocity**, and publishes them to an **MQTT broker** in JSON format.
